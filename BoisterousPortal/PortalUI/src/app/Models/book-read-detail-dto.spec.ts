import { BookReadDetailDto } from './book-read-detail-dto';

describe('BookReadDetailDto', () => {
  it('should create an instance', () => {
    expect(new BookReadDetailDto()).toBeTruthy();
  });
});
